var toastContainer;var toasts = new Map();function initalizeToastContainer(){toastContainer = document.createElement("div");toastContainer.className += "card-body toast-container d-flex justify-content-start align-items-end";toastContainer.ariaLive = "polite";toastContainer.ariaAtomic = "true";toastContainer.style.position = "fixed";document.getElementById("toastContainer").appendChild(toastContainer);}function createToast(name,id,text){let tst = document.createElement("div");tst.className += "toast";tst.role="alert";tst.ariaLive="assertive";tst.ariaAtomic="true";toastContainer.appendChild(tst);let tstheader = document.createElement("div");tstheader.className += "toast-header";tst.appendChild(tstheader);let tstname = document.createElement("strong");tstname.className += "me-auto";tstname.innerText=name;tstheader.appendChild(tstname);let cbtn = document.createElement("button");cbtn.type="button";cbtn.className += "ms-2 mb-1 close btn-close";cbtn.dataset.bsDismiss = "toast";cbtn.ariaLabel="Close";tstheader.appendChild(cbtn);let tstbody = document.createElement("div");tstbody.className += "toast-body";tstbody.innerText=text;tst.appendChild(tstbody);toasts.set(id,new bootstrap.Toast(tst));}function showToast(id){toasts.get(id).show();}function delToast(id){toasts.get(id)._element.remove();toasts.get(id).dispose();toasts.delete(id);}function addDELcallback(id){toasts.get(id)._element.getElementsByClassName("close")[0].addEventListener(onclick,()=>{delToast(id)})}function createAndShowToast(name,id,text){createToast(name,id,text);addDELcallback(id);showToast(id);}addEventListener("load",initalizeToastContainer);addEventListener("load",(ev)=>{createToast("Toast","Toast::toast_text","Loaded Successfully");showToast("Toast::toast_text");});